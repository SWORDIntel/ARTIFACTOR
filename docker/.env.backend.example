# ARTIFACTOR Backend Services Configuration
# Copy this file to /home/john/ARTIFACTOR/.env and customize

# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_DB=artifactor_v3
POSTGRES_USER=artifactor
POSTGRES_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# Generate secure password:
# openssl rand -base64 32

# ============================================================================
# Security Configuration
# ============================================================================
SECRET_KEY=CHANGE_ME_TO_256_BIT_KEY

# Generate secret key:
# openssl rand -hex 32

# ============================================================================
# Application Configuration
# ============================================================================
DEBUG=false
API_V1_PREFIX=/api
API_TITLE=ARTIFACTOR Backend API
API_VERSION=3.0.0

# ============================================================================
# Feature Flags
# ============================================================================
ML_CLASSIFICATION_ENABLED=true
SEMANTIC_SEARCH_ENABLED=true
COLLABORATION_ENABLED=true
PLUGIN_SYSTEM_ENABLED=true

# ============================================================================
# Performance Configuration
# ============================================================================
REDIS_CACHE_ENABLED=true
CACHE_TTL=3600
MAX_UPLOAD_SIZE=104857600

# ============================================================================
# Monitoring Configuration
# ============================================================================
METRICS_ENABLED=true
AUDIT_LOGGING_ENABLED=true

# ============================================================================
# CORS Configuration (Chrome Extension Support)
# ============================================================================
# Note: ALLOWED_ORIGINS is set in backend-compose.yml
# Default includes: http://localhost:3000, chrome-extension://*

# ============================================================================
# Usage Instructions
# ============================================================================
# 1. Copy this file to project root:
#    cp docker/.env.backend.example .env
#
# 2. Generate secure credentials:
#    SECRET_KEY=$(openssl rand -hex 32)
#    POSTGRES_PASSWORD=$(openssl rand -base64 32)
#
# 3. Edit .env and replace CHANGE_ME values
#
# 4. Start services:
#    ./docker/backend-services.sh start
#
# 5. Validate:
#    ./docker/validate-backend-services.sh
